# API 基础URL
# 开发环境：可以设置为完整 URL，如 http://localhost:8000/api/v1
# 生产环境：留空或设置为相对路径，通过反向代理访问（如 /api/v1）
# VITE_API_BASE_URL=http://localhost:8000/api/v1

# 图片等静态资源根 URL（可选）
# 仅当 API/静态 与前端不同域时设置，如 https://api.example.com；同域留空即可
# VITE_STATIC_BASE=
