# 应用配置
APP_NAME=动手学大模型 API
APP_VERSION=1.0.0
DEBUG=False

# 数据库配置
DATABASE_URL=sqlite:///./learning_platform.db

# JWT配置
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS配置（已设置为允许所有来源，无需配置）
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 项目根目录（可选，默认自动推断）
BASE_DIR=

# GitHub Webhook 配置（用于自动部署）
# 生成密钥: openssl rand -hex 32 (Linux/Mac) 或使用随机字符串生成器
WEBHOOK_SECRET=your-webhook-secret-key-here

# 微信网页授权（公众号 / 接口测试号）
# 须在公众平台「网页授权域名」中配置 WEB_APP_BASE_URL 的域名（仅域名，如 yourdomain.com 或 xxx.ngrok.io）
# 本地开发：微信不支持 localhost，需用内网穿透（ngrok、natapp）将前端暴露为 https 公网域名并配置
WECHAT_APP_ID=
WECHAT_APP_SECRET=
WEB_APP_BASE_URL=https://yourdomain.com
