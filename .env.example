# 项目根 .env 示例（供 Docker Compose 等读取，用于变量替换）
# 复制为 .env 后按需填写。compose 会使用 ${VAR} 注入到 backend 容器。

# 后端安全
SECRET_KEY=your-secret-key-change-in-production
WEBHOOK_SECRET=

# 审核通过时生成 README、Notebook、考核题所需（可选）
# 未设置时相关生成会跳过，章节仍会创建
OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o-mini
